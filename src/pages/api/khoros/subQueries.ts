const limit = 10;
export const subQueries = {
  labels: { limit },
  kudos: { limit },
  tags: { limit },
  tkb_helpfulness_ratings: { limit },
  images: { limit },
  videos: { limit },
  attachments: { limit },
  replies: { limit },
  ratings: { limit },
  custom_tags: { limit },
};
